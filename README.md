<!-- 1. Can clone the repo / fork
2. Push the source to Web / Mobile / Backend directory

To run the Mobile:
```bash
cd frontend
Docker compose down
Docker compose up --build 

Another terminal:
cd frontend
Flutter run -d Chrome

To run the CMS
```bash
npm install
npm start



-->